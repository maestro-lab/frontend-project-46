const genDiff = (filepath1, filepath2, format = 'stylish') => {
  // Пока заглушка, будем реализовывать позже
  return `Comparing ${filepath1} and ${filepath2} with format: ${format}`;
};

export default genDiff;